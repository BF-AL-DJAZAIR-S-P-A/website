<style>
.star-rating {
  direction: rtl; /* pour que les étoiles se remplissent de gauche à droite */
  display: inline-flex;
}

.star-rating input {
  display: none;
}

.star-rating label {
  font-size: 2rem;
  color: #ccc;
  cursor: pointer;
}

.star-rating input:checked ~ label {
  color: gold;
}

.star-rating label:hover,
.star-rating label:hover ~ label {
  color: gold;
}
</style>
{{ form_start(formNote) }}

<div class="star-rating">
    {% for i in 1..5 %}
        <input type="radio" id="star{{ i }}" name="{{ form.valeur.vars.full_name }}" value="{{ i }}"
            {% if form.valeur.vars.value == i %}checked{% endif %}>
        <label for="star{{ i }}">★</label>
    {% endfor %}
</div>

<button class="btn">{{ button_label|default('Save') }}</button>

{{ form_end(formNote, {'render_rest': false}) }}