{% extends 'base.html.twig' %}

{% block title %}Appels{% endblock %}

{% block body %}
    <h1>Appels</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ appel.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ appel.titre }}</td>
            </tr>
            <tr>
                <th>Texte</th>
                <td>{{ appel.texte }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ appel.image }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ appel.date ? appel.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_appels_index') }}">back to list</a>

    <a href="{{ path('app_appels_edit', {'id': appel.id}) }}">edit</a>

    {{ include('appels/_delete_form.html.twig') }}
{% endblock %}
