{{ form_start(formNote) }}
    {{ form_row(formNote.valeur) }}
    {# Si tu veux afficher aussi candidatures, tu le mets ici : #}
    {# {{ form_row(form.candidatures) }} #}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(formNote) }}


